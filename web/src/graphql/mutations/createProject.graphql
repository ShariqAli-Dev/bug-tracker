mutation CreateProject($options: CreateProjectInput!) {
  createProject(options: $options) {
    id
    name
    description
    createdAt
    updatedAt
  }
}
mutation CreateProject($options: CreateProjectInput!) {
  createProject(options: $options) {
    id
    name
    description
    createdAt
    updatedAt
  }
}
