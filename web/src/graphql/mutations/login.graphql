mutation Login($options: UserLogin!) {
  login(options: $options) {
    errors {
      field
      message
    }
    user {
      id
      email
      role
      name
      createdAt
      updatedAt
    }
  }
}
