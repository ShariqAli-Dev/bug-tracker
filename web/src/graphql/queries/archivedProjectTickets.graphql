query ArchivedProjectTickets($projectId: Float!) {
  archivedProjectTickets(projectId: $projectId) {
    id
    projectId
    creator
    title
    description
    priority
    type
    status
    archived
    updatedAt
  }
}
